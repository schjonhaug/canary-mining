# Pool Logo Sources

These SVGs are copied from the public `mempool/mining-pool-logos` repository:

https://github.com/mempool/mining-pool-logos

The pool attribution heuristic follows the `mempool/mining-pools` registry shape:
match known coinbase payout addresses first, then regex-match coinbase script tags,
then fall back to `Unknown`.

https://github.com/mempool/mining-pools
